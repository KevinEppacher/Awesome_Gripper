controller_manager:
  ros__parameters:
    update_rate: 100  # Hz

    gripper_controller:
      type: position_controllers/JointTrajectoryController

gripper_controller:
  ros__parameters:
    joints:
      - gripper_joint_1_right
    command_interfaces:
      - position
    state_interfaces:
      - position
      - velocity
